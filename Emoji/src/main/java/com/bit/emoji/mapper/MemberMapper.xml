<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="Member">
	<insert id="insertMember" parameterType="com.bit.emoji.model.MemberVO">
		insert into member
		values (#{memberNum}, #{memberEmail}, #{memberName}, #{memberPassword}, #{memberGender}, #{memberPhoneNum}, #{memberRegDate}, #{memberJoinGrade}, #{memberCapGrade})
	</insert>
	
	<select id="selectMemberByMemberEmail" parameterType="String" resultType="String">
		select memberPassword from member where memberEmail=#{memberEmail}
	</select>
	
	
</mapper>
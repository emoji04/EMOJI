<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="Mypage">
	<!-- 회원정보 상세조회 -->
	<select id="listAll" resultType="com.bit.emoji.model.MemberVO" parameterType="String">
	select
		memberEmail, memberName, memberPassword, memberGender, memberPhoneNum, memberJoinGrade, memberCapGrade
		from
		member
        where memberEmail = #{memberEmail}

	</select>
	<!-- 회원정보수정 -->
	<select id="update" resultType="com.bit.emoji.model.MemberVO">
		UPDATE emoji.member SET
		memberPassword=#{memberPassword},
		WHERE memberEmail=#{memberEmail}

	</select>

</mapper>